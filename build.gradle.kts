plugins {
    id("root.passwordgenerator")
}

allprojects{
    description = "A simple but secure password generator"
    group = "com.github.zseaborn63.passwordgenerator"
    version(
        major = 0,
        minor = 1,
        patch = 0,
        releasebuild = 0
    )
}